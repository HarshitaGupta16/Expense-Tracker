import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
// whatever is there in App will replace the content inside div whos id is root 
// inside index.html file
